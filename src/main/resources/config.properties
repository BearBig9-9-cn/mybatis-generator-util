jdbc.driverClass = com.mysql.jdbc.Driver
jdbc.url = jdbc:mysql://127.0.0.1:3306/test
jdbc.user = root
jdbc.password = root

# 生成实体类所在的包
package.model=com.bearbig.model
# 生成 mapper 类所在的包
package.mapper=com.bearbig.dao
# 生成 mapper xml 文件所在的包，默认存储在 resources 目录下
package.xml=sqlmapper